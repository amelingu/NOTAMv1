# NOTAM Briefing — Python dependencies
# Install inside a virtual environment:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   pip freeze > requirements.txt   # update pins after any upgrade

certifi==2025.4.26
